version: '3'
services:
  jira:
    image: cptactionhank/atlassian-jira:7.11.0
    networks:
      - dmz
    environment:
      - X_PROXY_SCHEME=https
      - X_PROXY_NAME=jira.ericksonmarine.net
      - X_PROXY_PORT=443
    restart: always

networks:
  dmz:
    external:
      name: dmz
